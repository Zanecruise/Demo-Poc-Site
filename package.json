{
  "name": "demo-poc-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "esbuild index.tsx --bundle --outfile=dist/bundle.js --jsx=automatic --servedir=dist --watch",
    "build": "esbuild index.tsx --bundle --outfile=dist/bundle.js --jsx=automatic --minify && cp index.html dist/index.html",
    "preview": "npx serve dist -l 4173"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "typescript": "^5.0.0"
  }
}
